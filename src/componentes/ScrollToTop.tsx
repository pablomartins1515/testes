import React, { MouseEvent } from 'react';

export function ScrollToTop(event: MouseEvent<HTMLButtonElement>) {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });

  console.log('Botão clicado!')
      return (
      
        <div >          
        </div >
    )
}

